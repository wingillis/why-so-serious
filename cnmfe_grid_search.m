function cnmfe_grid_search()
  %% the intention of this file is to go through many different parameters and
  % see which sets of parameters optimally extracts neurons

  % for the grid search, construct the params and save them into some
  % mat file and read in the matfile when running a grid search on the
  % recordings

end % function
